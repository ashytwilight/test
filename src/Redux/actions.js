import { createAction } from '@reduxjs/toolkit';

export const setAge = createAction('ageSlice/setAge');
export const deleteAge = createAction('ageSlice/deleteAge');
export const setPrice = createAction('priceSlice/setPrice');
export const deletePrice = createAction('priceSlice/deletePrice');
